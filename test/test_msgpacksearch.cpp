#include <utility>
#include <variant>

#include <gtest/gtest.h>

#include "msgpacksearch.h"

using namespace msgpacksearch;

TEST(parse, integers)
{
    std::vector<uint8_t> data = {}
    Msgpack data();

    
}

TEST(config, second)
{
    EXPECT_EQ(1, 1);
}